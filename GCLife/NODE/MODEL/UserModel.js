OVERRIDE(GCLife.UserModel, function(origin) {
	'use strict';

	GCLife.UserModel = OBJECT({

		preset : function() {
			return origin;
		},

		init : function(inner, self, params) {
			
			
		}
	});
});
